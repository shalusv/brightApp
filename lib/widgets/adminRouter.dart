import 'package:flutter/material.dart';

class AdminRouter extends StatelessWidget {
  final String screen;

  AdminRouter({this.screen});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
